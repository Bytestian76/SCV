[Definition]
# Filtro para detectar intentos de login fallidos en el backend de SCV (FastAPI)
# Busca líneas que coincidan con el formato de logger.warning() definido en auth.py

failregex = Intento de inicio de sesión fallido:.*desde la IP <HOST>
            Intento de inicio de sesión rechazado:.*desde la IP <HOST>

ignoreregex =
